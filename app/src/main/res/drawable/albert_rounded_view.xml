<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:drawable="@drawable/albert1">
    </item>

    <item>
        <shape
            android:padding="10dp"
            android:shape="rectangle">
            <corners android:radius="200dp" />
            <stroke
                android:width="3dp"
                android:color="@color/white" />
        </shape>
    </item>

</layer-list>

<!-- This is a layer-list, means you have to add big layer downside which is to be cut. Like albert1 image is big layer here, later, when we will add the shape and it's properties over it (which is a smaller layer in terms of area), it will cut the extra area of its previous layer. And hence, we get combination of 2 layers here.-->